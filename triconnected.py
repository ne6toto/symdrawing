def triconnected():
    #call Fontet's algorithm
    #choose and orbit of min size
    #perform star triangulation
    #transform graph to get an outside face with >3 edges
    #apply Fontet's algorithm again
    #obtain generators
